class menu:
	def __init__(self):
		self.menuitems={}
	def additems(self,name,cost):
		self.menuitems[name]=cost
	def show(self):
		for a,b in (self.menuitems.items()):
			print(a,b)
	def showitem(self,item):
		price=self.menuitems.get(item)
		print(price)
