class menu:
	def __init__(self):
		self.l={}
	def additems(self,name,cost):
		self.key=name
		self.value=cost
		self.l.update({self.key:self.value})
	def show(self):
		for a,b in (self.l.items()):
			print(a,b)
	def showitem(self,item):
		price=self.l.get(item)
		print(price)
